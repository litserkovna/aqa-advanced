const rectangleLength = 12.255;
const rectangleWidth = 45.507;
const rectangleArea = rectangleLength * rectangleWidth;
console.log(
  'Rectangle area with the length',
  rectangleLength,
  'and width',
  rectangleWidth,
  '=',
  rectangleArea.toFixed(2)
);
