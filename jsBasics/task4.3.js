const cylinderRadius = 25;
const cylinderHeight = 2;
const cylinderVolume = Math.PI * Math.pow(cylinderRadius, 2) * cylinderHeight;
console.log(
  'Cylinder volume with the radius',
  cylinderRadius,
  'and heigh',
  cylinderHeight,
  '=',
  cylinderVolume.toFixed(2)
);
